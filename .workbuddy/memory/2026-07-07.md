# 2026-07-07

## 重建个人主页
- 将网站从"陶红希"改名为"Yue-xin G."
- 重写 `index.html`：简约现代风格，单页设计，包含 Hero、About、Skills、Education、Contact 五个部分
- 重写 `README.md`：更新为 Yue-xin G. 的描述
- 移除了原版的深色模式、复杂动画、时间轴等元素，改用极简卡片式布局
- 技术栈：纯 HTML + CSS，Google Fonts (Inter + Noto Sans SC)

## 升级为 Vue 3 + Vite
- 将静态 HTML 页面迁移到 Vue 3 + Vite 架构
- 创建 `package.json`、`vite.config.js`、`src/main.js`、`src/App.vue`
- 拆分为 7 个组件：NavBar、HeroSection、AboutSection、SkillsSection、EducationSection、ContactSection、FooterSection
- 使用 `<script setup>` 语法和 Composition API
- 全局样式提取到 `src/styles/main.css`
- 构建成功：dist/index.html (0.80 kB) + CSS (6.18 kB) + JS (69.70 kB)
- 开发命令：`npm run dev`，构建命令：`npm run build`
